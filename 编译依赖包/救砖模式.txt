

短接TP1 TP2，打开电源

# 1. 清理 U-Boot (防止 U-Boot 本身的环境变量乱了)
./build.sh clean uboot
./build.sh uboot

# 2. 清理 Kernel (关键！)
./build.sh clean kernel
./build.sh kernel

# 3. 重新打包
./build.sh firmware
./build.sh updateimg